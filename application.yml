spring:
application:
name: Config-Server-Default

eureka:
   instance:
     prefer-ip-address: true
client:
   fetch-registry: true
   register-with-eureka: true
   service-url:
   defaultZone: http://localhost:8761/eureka


creditcard:
  service:
    url: http://localhost:7006/customers/{id}
